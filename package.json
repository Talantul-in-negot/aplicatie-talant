{
  "name": "talant",
  "version": "1.0.0",
  "description": "",
  "main": "questions.js",
  "scripts": {
    "build": "node build-site.js",
    "test": "node verify-questions.js && node verify-project.js && node scripts/generate-secure-scoring-migration.js",
    "test:e2e": "node e2e-live.js",
    "test:db": "node scripts/verify-migrations.js",
    "keys:generate": "node scripts/generate-secure-scoring-migration.js --write",
    "db:apply": "node apply-migrations.js",
    "deploy": "npm run build && dotenv -e .env -- wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.92.0",
    "dotenv-cli": "^11.0.0",
    "wrangler": "^4.127.1"
  }
}
